## Why we need to use `className` instead of `class` for JSX?

- In JS, `class` keyword is already reserved for creating ES6 classes. Hence JSX had to define a new keyword called `className` to define HTML class names to avoid any collision

### Resources

- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes
- https://www.geeksforgeeks.org/why-react-uses-classname-over-class-attribute/
