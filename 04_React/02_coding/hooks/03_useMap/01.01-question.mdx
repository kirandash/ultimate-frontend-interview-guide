## useMap

### Level: Medium, Time: 20 Minutes

### Description

- Create a custom `useMap` function that works similar to state but for a map object
- Input: takes an `initialvalue` and
- Output: should return an object with map and fns to set, delete and clear
