## Pre Solution

### Techniques:

- closure (not storing values)
- `this` keyword with arrow fn
- `Function.prototype.apply()` or `Function.prototype.call()`
  - we used call because we needed additional arg which is the callback fn
- `Promise`
  - resolve, reject
